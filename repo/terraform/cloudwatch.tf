# cloudwatch.tf
# Owner: Person B
# Purpose: Logging + alarms so you can screenshot monitoring in action
# (e.g. an alarm firing on high CPU, which also ties into auto scaling).

# TODO: aws_cloudwatch_log_group (for app logs)
# TODO: aws_cloudwatch_metric_alarm (high CPU on the ASG -> triggers scale-out)
